# VibePet 更新日志

## v1.2.14 (2026-07-20)

### 新增功能
- **全新「墨团小猫」皮肤**：参考羊毛毡质感深蓝灰小猫形象（翠绿大眼、猫耳、胡须、尾巴），程序化渲染（`tools/generate_cat_assets.py`），6 种状态全部拥有独立形态与 24 帧循环动画——含飞机耳（angry）、垂耳（tired）、尾巴摇摆/翘起、眨眼、呼吸等细节。
- **双皮肤切换**：设置 → 宠物外观新增「宠物形象」下拉选项，可在「墨团小猫」与「果冻史莱姆」之间随时切换；旧史莱姆形象完整保留于 `assets/skins/slime/`，皮肤化架构后续可扩展更多形象。
- 应用图标更换为小猫形象。

---

## v1.2.13 (2026-07-20)

### 美术与动效全面升级
- **全新果冻史莱姆形象**：弃用像素风贴图，改用程序化渲染（`tools/generate_pet_assets.py`，4 倍超采样抗锯齿）的果冻质感史莱姆——垂直渐变半透明身体、径向高光、柔和光泽、大眼睛与腮红，6 种状态各有专属配色与表情。
- **动效大幅丰富**：6 种状态全部由简单几帧升级为 **24 帧循环动画**（70ms/帧）：
  - idle：呼吸式挤压拉伸 + 周期性眨眼 + 轻微摇摆
  - work：打字式快节奏弹跳 + 四周灵感火花闪烁
  - happy：高弹跳（落地挤压/腾空拉伸）+ 爱心漂浮上升
  - tired：厚重下垂眼皮 + 疲惫摇晃 + 汗滴交替滑落
  - sleep：趴扁深呼吸 + Zzz 依次飘出放大淡出
  - angry：高频震动 + 怒气符号脉冲 + 蒸汽上升
- **GIF 画质优化**：全局共享调色板 + Floyd-Steinberg 抖动量化，消除渐变色带与帧间闪烁；新增落地接触阴影。
- 图标 `icon.png` / `icon.ico` 同步重绘；气泡/面板布局锚定比例按新形象实测更新。

---

## v1.2.12 (2026-07-20)

### 修复
- **修复截图后粘贴失败的严重冲突**：修复了「划词字数统计」功能在鼠标拖拽松手时向全局注入 `Ctrl+C` 并读写剪贴板的缺陷。截图（Snipaste/微信截图）同样依靠拖拽选区触发，松手瞬间该功能会抢占并还原剪贴板，把截图工具刚写入的图片直接覆盖，导致截图后无法粘贴。现在当检测到截图工具在前台时，划词统计将完全不注入按键、不触碰剪贴板。
- **截图冲突规避支持微信截图**：原冲突规避仅识别 Snipaste 进程名，无法检测微信截图（其窗口属于 WeChat 进程）。现新增窗口类名识别（`SnapShotWnd`），并同时覆盖 Windows 自带截图工具与 Win+Shift+S 系统截图。
- **截图避让响应大幅提速**：截图工具检测从原先的 5~10 秒轮询改为 Win32 前台窗口事件钩子即时触发，桌宠与便签在截图开始的瞬间隐藏、结束后瞬间恢复。
- **恢复逻辑修正**：临时隐藏前记录桌宠与便签各自的可见状态，截图结束后只恢复原本可见的窗口，不再把用户手动隐藏的窗口误弹出。

### 优化
- **前台窗口自愈式复核**：监控线程每轮对前台窗口进程与截图状态做一次兜底复核，即使事件钩子丢失个别事件，也能在一个轮询周期内自动纠正，状态追踪更稳定。
- **划词统计自我避让**：前台窗口为本程序自身对话框（设置/统计等）时，划词统计自动跳过，避免对自家窗口注入按键。

---

## v1.2.11 (2026-07-01)

### 性能与优化
- **动态采样间隔与空闲降噪**：引入空闲检测，在用户处于空闲状态（无输入 5 分钟以上）时自动将系统监控轮询间隔加倍（如由 5s 增加到 10s），恢复活跃时恢复为配置采样值，空闲下的累计时间已同步使用动态间隔，整体空闲资源消耗降低 50%。
- **日志采样式写入**：将日志级别默认设为 `WARNING` 以最大化避免 I/O 写入；重要状态变化（如 idle 与 active 的来回切换）通过 `logger.info` 进行提示，避免无用日志频繁输出阻塞磁盘。
- **更新镜像源优化**：默认将检查更新的镜像源修改为“直连 GitHub”，以提高 GitHub 用户或配备科学上网环境时的访问直达度。

### 修复与完善
- **修复 PyInstaller 打包失败的缺陷**：修复了 `core/monitor.py` 在 `run` 循环内部的缩进语法错误，该错误先前会导致 PyInstaller 在打包时报错 `ModuleNotFoundError: No module named 'core.monitor'` 并损坏 EXE 程序。
- **修复单实例误弹窗**：修复了 `main.py` 在单实例锁定成功（未重复启动）时依然错误弹出“重复启动”提示的 `if-else` 逻辑缩进缺陷。

---

## v1.2.10 (2026-06-06)

### 修复
- **强制触发 100% 概率随机情绪**：修复了当某种情绪概率被设为 100% 时，如果在活跃时段（例如用户在操作设置或处于非空闲状态）会被 `is_idle` 条件拦截、导致无法立即触发睡眠或开心等动画的缺陷。现在当概率设为 100% 时将无条件立即强制切换，彻底符合直觉。

---

## v1.2.9 (2026-06-06)

### 修复
- **修复随机情绪概率设置与开关失效问题**：
  - **永久状态重置**：修复了当某种情绪概率被设为 100% 触发为永久模式后，修改配置调低概率时，桌宠依然被无限期卡在永久状态（`float('inf')`）无法自动退出的严重逻辑缺陷。现在当概率变动不再为 100% 时，将自动清理该永久状态。
  - **开关彻底停用**：修复了当用户在设置页面关闭“启用随机情绪”开关后，若当前正处于随机情绪状态下，该状态仍会一直存续的缺陷。现在关闭该开关时，将立即且无条件地将活动状态重置为正常状态。

---

## v1.2.8 (2026-06-06)

### 性能与流畅度优化
- **消除启动与检测更新卡顿**：
  - **异步检测更新**：将启动自动检测更新及手动检测更新从主 GUI 线程移入独立的后台子线程（`UpdateCheckThread`），避免阻塞主线程导致界面冻结。
  - **异步数据清理**：将数据库初始化后的旧数据降维合并（`auto_cleanup_data`）及大开销的 `VACUUM` 磁盘压缩操作移入后台 `threading.Thread` 中异步执行，彻底解决应用启动时的短暂卡死现象。

---

## v1.2.7 (2026-06-05)

### 美术与形象还原
- **还原经典史莱姆 (Slime) 形象**：
  - 根据用户反馈，重新启用经典的史莱姆 (Slime) 动画 GIF 和对应的 PNG 静态资源，实现桌面端与网站端一致的生动形象。

---

## v1.2.6 (2026-06-05)

### 美术与形象升级
- **太空科技猫咪全新形象上线**：
  - 更新桌宠形象外观，替换了原有的 Slime (史莱姆) 基础形象，更换为全新的**“太空科技猫咪 (Sci-Fi Cyber Cat)”**形象。
  - 针对 `idle` (空闲)、`work` (工作)、`happy` (开心)、`tired` (疲惫)、`sleep` (睡眠)、`angry` (愤怒) 6类状态进行了全面的静止头像与外观更新。

---

## v1.2.5 (2026-06-04)

### 修复
- **下拉选择窗口 (QComboBox) 显示异常修复**：
  - 修复了在系统处于暗黑模式下，即使界面主题风格设为明亮模式，下拉选择框弹出项的背景依然保持黑色，导致与暗灰色选项文字极难分辨的问题。
  - 强制在下拉列表视图 `QListView` 实例上直接应用与主题匹配的样式表，成功屏蔽系统原生暗黑主题对弹窗背景的影响，确保各个模式下的下拉菜单文字清晰可读。

---

## v1.2.4 (2026-06-04)

### 功能优化
- **下载更新加速 (解决下载慢)**：
  - 新增“下载加速通道”设置项，支持“自动选择 (多镜像测速)”及多个手动加速通道。
  - 在下载前通过多线程并发对国内多个主流 GitHub 加速镜像（如 Moeyy、GHProxy 等）进行连接测速，并动态选择延迟最低的可用通道，大幅提升下载成功率和速度。
  - 保留官方备用服务器作为最终的自动故障转移 fallback 方案。

---

## v1.2.3 (2026-06-04)

### UI 优化
- **窗口最小化支持**：
  - 为「系统设置」和「统计看板」窗口添加了标准的最小化按钮。
  - 为「便签待办清单」无边框窗口设计并添加了自定义的 `－` 最小化按钮及配套的主题切换样式，点击后可成功最小化到后台。当通过桌宠右键菜单或托盘开关切换时，会自动从最小化状态平滑恢复显示。
- **疲劳强制提醒窗口置顶及最小化支持**：
  - 修复了健康警示窗口因为没有 stays-on-top 且未强制激活导致只显示在任务栏的 Bug。现在它会强制保持最前并弹出在前台，同时支持窗口最小化，避免阻挡屏幕。
  - 动态格式化显示真实的限制时间（如 “2.5 小时” 或 “120 分钟”），替代原本硬编码的 “8 小时” 错误描述。

### 修复
- **阈值延长提醒无响应修复**：
  - 修复了用户在触发今日提醒阈值后，前往设置页手动调大/延长阈值（如从 120 分钟改为 150 分钟），由于 30 分钟 Cooldown 限制导致新阈值达到时无法再次触发弹窗的 Bug。现在只要检测到阈值修改或开关重新打开，对应的冷却状态便会立即重置，实现立刻响应。
- **稳定性增强**：
  - 针对桌宠偶发的退出/闪退现象，优化了 `win10toast` 核心代码。在 PyInstaller 打包环境下载入通知默认图标可能会因为文件缺失引发背景线程崩溃；现强制指定加载项目自带的 `assets/icon.ico`，彻底排除了通知背景线程的异常风险。

---

## v1.2.2 (2026-06-03)

### 修复
- **数据隐私保护修复**：
  - 修复了自动化构建打包脚本 `build_release.py` 在打包发布 ZIP 归档时误将包含个人配置、软件使用记录、调试日志等敏感数据文件（`config.json`, `usage.db`, `vibe_pet.log`, `vibe_pet.lock`）打包进发布压缩包的问题，全面隔离并保护用户隐私数据。

---

## v1.2.1 (2026-06-03)

### UI 优化
- **监控气泡紧贴宠物头顶**：
  - 重新统一了初始化与动态调整中气泡区域的计算参数，消除了构造函数与 `set_pet_size` 之间的数值不一致（`bubble_height` 从 `max(45, 20%)` 统一缩减至 `max(20, 10%)`），使监控气泡紧贴宠物头顶仅 1px 间距，视觉距离自然紧凑。
- **窗口占用面积进一步缩减**：
  - 初始化时窗口高度比上版本再减少约 20px（200px 宠物尺寸下窗口从 ~240 降至 220），最大程度避免透明窗口阻挡屏幕操作。

### 修复
- **健康警告窗口标题栏图标缺失修复**：
  - `trigger_fatigue_warning` 和 `open_about_dialog` 对话框优先使用 `.ico` 格式图标文件，解决 Windows 标题栏左上角图标不显示的问题。

---

## v1.2.0.1 (2026-06-02)

### 交互优化与整体面积缩减
- **桌宠透明空边剔除（资产裁剪）**：
  - 批量裁剪了 `assets/` 下所有的 GIF 动画和静态 PNG 宠物图片的冗余透明外边距，剔除了高达 37% 的无效空间，使宠物本身更清晰、widget footprint 更小。
- **整体屏幕面积占用大幅缩减**：
  - 将桌宠逻辑窗口占用的屏幕面积缩减了 32%，同时由于贴图边距的去除，选择较小的宠物尺寸（如 100px）时能保持极佳的可见性，使占用空间减少达 4 倍，完全解决大窗口阻挡屏幕操作的问题。
- **监控气泡大小/字号修改**：
  - 在外观设置中增加了「气泡大小 (字号)」选项（支持 8pt-20pt 自由调节），满足不同屏幕分辨率用户的清晰度诉求。
- **动态头顶气泡位置精调 (reposition_components)**：
  - 重构了子组件定位逻辑。无论桌宠大小如何、在什么动画状态（如躺下的 sleep 状态 vs 站立的 idle 状态）、以及气泡字号多大，监控气泡与普通对话气泡都会自动自适应计算宠物头顶边缘，稳定保持在宠物头顶上方 2 像素的位置，视觉距离极其自然紧凑。

---

## v1.2.0 (2026-06-02)

### 新增功能与交互优化
- **便签待办清单边缘吸附（贴边隐藏）**：
  - 便签待办清单悬浮窗新增跟桌宠一样的边缘吸附（躲猫猫）模式。拖拽至屏幕左右侧边缘 20px 以内时自动缩进收起，仅露出 15px 边缘。
  - 点击露出的边缘，便签窗口将平滑滑出还原。
- **统计看板增加「待办清单」（进行中待办）**：
  - 统计看板中新增「待办清单」标签页，用于查看和管理当前所有未完成的待办事项。
  - 在该表格中支持与桌面浮动窗口实时联动的 CheckBox 勾选标记完成，并在勾选后自动重新统计和移入「待办日记」。
- **待办列表多选与批量删除**：
  - 在统计看板的「待办清单」（进行中）和「待办日记」（已完成）两个表格中均支持 Shift/Ctrl 鼠标框选多行记录，并支持一键「批量删除所选」。
- **分类占比饼图标签防重叠**：
  - 分类占比饼图在渲染时，将自动隐藏占比小于 3% 的小占比分类标签，规避多小分类时的文本碰撞重叠。
- **饼图增加详尽悬停 ToolTip**：
  - 饼图新增高质感鼠标悬停 ToolTip 提示，移动到切片上可实时显示该分类的详细名称、已监控总时长和具体比例（%），方便查看小占比分类。

### 修复
- **Snipaste 截图软件冲突规避**：
  - 修复了因为桌宠和便签置顶层级导致与 Snipaste 截图工具热键冲突、截图卡顿或画面覆盖的问题。
  - 引入了自动窗口感知逻辑，在监测到前台为 Snipaste 截图时主动静默退避隐藏，截图结束/取消后瞬间自愈恢复显示。
- **规则特有状态动画卡死（永久冒火）修复**：
  - 修复了因为每日累计使用时间或游戏极限时长条件整天成立，导致桌宠一旦触发警报就永久卡在生气（冒火）或疲惫状态的 Bug。
  - 现已引入 60 秒限时生效机制，规则警告动画播放 60 秒后将自动平息并恢复正常，不再无限冒火。

---

## v1.1.9 (2026-06-02)

### 优化与修复
- **国内更新下载加速**：新增 `mirror.ghproxy.com` 国内高速 GitHub 代理，自动在后台优先尝试加速下载更新包，解决国内用户直连 GitHub 下载极慢、易超时的问题。
- **自动更新替换逻辑修复（文件锁绕过）**：
  - 修复了旧版本更新时由于主进程未完全退出完毕、文件被系统锁定导致更新覆盖失败并退回老版本的 Bug。
  - 新引入了 `.bak` 临时文件重命名覆盖技术，强制瞬间释放可执行文件占用锁。从 v1.1.9 起，后续版本更新将百分百替换成功。

---

## v1.1.8 (2026-06-02)

### 新增功能
- **统计看板支持日期选择器**：
  - 在统计看板顶部新增日期选择控件（带日历弹出），支持跳转到任意日期查看历史数据。
  - 新增「今天」快捷按钮，一键返回查看当日数据。
  - 柱状图、饼图、待办日记三个标签页均会根据所选日期动态刷新数据。
- **待办日记增加删除按钮**：
  - 在统计看板「待办日记」标签页的每行记录右侧新增 🗑️ 删除按钮。
  - 点击删除按钮会弹出确认对话框，确认后物理删除该条记录。

---

## v1.1.7 (2026-06-02)

### 新增功能
- **删除已完成任务确认弹窗（归档与删除分流）**：
  - 清除**未完成**的任务时，不会记录在后台，直接彻底物理删除。
  - 清除**已完成**的任务时，会弹出对话框询问用户"是否在后台保存该已完成的待办事项为历史日记？"。若选择"是"则归档保存，若选择"否"则物理删除不予保存，选择"取消"则中止删除。
- **统计看板新增"待办日记"展示区域**：
  - 在统计看板（今日记录）的界面中，增加了一个全新的"待办日记"标签页。
  - 该标签页以表格形式展示用户所有已完成的待办任务，清晰显示任务的当前状态（如📁 已归档 / ✅ 已完成）、任务的具体内容，以及准确的完成时间，形成专属于您的效率成长日志。
- **删除"清除已完成"按钮**：
  - 移除了标题栏中的"清除已完成"批量清除按钮，将归档/删除的选择控制权精细化交给用户对每个条目的独立操作，避免误伤。

---

## v1.1.6 (2026-06-02)

### 新增功能
- **待办任务后台归档与历史留存（像日记一样记录）**：
  - 勾选完成并从列表中删除（点击 `×`）或使用新推出的"🧹 清除已完成"按钮归档的待办任务，**不会从数据库中被抹去**。它们会保留在本地 `usage.db` 的 `todo` 表中（标记 `archived = 1`），成为您在 VibePet 里留存的工作成就历史和努力的日记见证。
  - 只有那些在未完成状态下直接被删除（点击 `×`）的任务，才会被物理删除，避免无效、废弃的信息占用数据库资源。
- **清除已完成功能 (Clear Completed)**：
  - 便签窗口顶部新增了一个精致的小按钮 "🧹 清除已完成"，点击可一键清除所有已完成任务，清空界面的同时自动在后台进行归档留存。

### 优化与修复
- **滚动条布局裁剪修复**：通过将便签内容标签的大小策略调整为 `Ignored` 并设定最小收缩宽度，修复了当输入超长任务名时会导致布局溢出、遮挡右侧 `×` 删除按钮的界面缺陷。现在无论任务多长，删除按钮均能稳稳显示。

---

## v1.1.5 (2026-06-02)

### 新增功能
- **桌宠与待办事项智能联动**：
  - **即时交互反馈**：新增待办任务时，桌宠会随机展示鼓励气泡并播放开心/眨眼动画；勾选完成待办时，桌宠会向您表示祝贺（播放开心动画）。
  - **全部完成特殊庆祝**：当您清除并搞定列表中所有待办任务时，桌宠会触发专属高频开心欢呼气泡，提供满满的自豪感。
  - **日常随机提醒**：如果今天还有未完成的任务挂在清单上，桌宠会在日常运行中（10% 的随机概率）念出具体的任务名称督促您去完成，不再是一个冰冷的列表。

---

## v1.1.4 (2026-06-02)

### 新增功能
- **便签待办清单 (Sticky Notes Todo List)**：
  - 新增独立的、拟物化风格桌面待办便签窗口，可在桌面上自由拖拽并记录待办任务
  - 自适应明亮/暗黑主题：明亮模式下呈现为浅黄 Post-it 便签，手写感文字；暗黑模式下呈现为淡紫灰色高质感便签
  - 便签内容自动持久化保存至本地 SQLite 数据库，任务状态实时保存，支持新增、删除与勾选
  - 支持智能沉底：已完成任务自动下沉至列表底端，未完成任务在上方优先展示，方便聚焦工作
  - 在桌宠右键菜单与系统托盘菜单中均集成了「便签待办」开关，窗口显示状态支持开机记忆与自愈
  - **定位与设置修复**：将便签窗口设为独立顶级窗口以应用屏幕绝对坐标，修复偏移 Bug；并在系统设置"宠物外观"选项卡中加入了"显示桌面便签待办清单"的控制开关。

---

## v1.1.3 (2026-06-02)

### 新增功能
- **边缘隐藏（躲猫猫）模式 (Screen Border Snapping)**：
  - 当桌宠被拖拽至屏幕最左侧或最右侧边缘（小于 20px 阈值）时，自动滑动收缩隐藏，仅保留 35px 小部分身体可见，不占用屏幕空间
  - 点击隐藏的露出部分，桌宠将平滑滑动展开恢复正常状态
  - 隐藏状态下自动暂停呼吸与眨眼动画，并隐藏一切文字与监控气泡，防止视觉突兀
  - 在设置 → 宠物外观中，新增「启用贴边自动隐藏（躲猫猫模式）」控制选项，支持随时开启/关闭，并随外观设置一键恢复默认值
  - 动态适配尺寸调整，如果在隐藏状态下修改尺寸或禁用该功能，桌宠将自动恢复展开状态，避免定位错误

---

## v1.1.2 (2026-06-02)

### 优化与性能提升
- **底层架构事件驱动重构**：引入 Win32 `SetWinEventHook` 钩子，从原先的 2 秒轮询 `GetForegroundWindow` 彻底重构为事件驱动型前台窗口变更回调，彻底消除后台空转时的 CPU 进程查询开销
- **原生 GIF 动画播放**：废弃手动单帧切图，通过 PyQt6 `QMovie` 完美支持 assets 目录下 `.gif` 格式动画（如 `work.gif` 等）的直接播放，支持动态调整大小与静态 PNG 的无缝 Fallback 降级

---

## v1.1.1 (2026-06-02)

### 界面与体验优化
- **按键触感优化**：设置页面所有按钮（如检测版本、重置等）优化为拟物化微交互状态，增强悬停、按下时的边缘阴影与文字高亮效果，手感更加灵敏自然
- **下拉框对比度修复**：解决深色主题下分类下拉选择框文字与背景对比度偏低、难以看清的问题
- **划词统计优化**：简化字数累计提醒，去除悬浮卡片模式，只保留桌宠旁边的气泡跟随方式

---

## v1.1.0 (2026-06-02)

### 新增功能
- **划词字数累计**：新增全局鼠标划词字数统计功能，可在设置中选择开启或关闭
- **默认主题变更**：桌宠默认主题切换为更为温和干净的「明亮主题」

---

## v1.0.6 (2026-06-01)

### 新增功能
- **滚轮防误触**：设置页面滚动时，鼠标滚轮不再误触改变概率输入框的值

### 修复
- 修复设置页面滑动时容易误触修改随机情绪概率的问题

---

## v1.0.5 (2026-05-31)

### 新增功能
- **GitHub 项目链接**：关于页面新增可点击的 GitHub 仓库链接
- **检测更新**：
  - 启动后 5 秒自动检测 GitHub Releases 最新版本
  - 设置 → 关于页面新增「🔍 检测更新」手动检测按钮
  - 发现新版本时气泡提示，附带下载链接

### 修复
- 修复未分类进程分类后，统计看板仍显示为"其他"的问题（分类后立即生效）
- 修复未分类进程分类按钮与背景色相同、无点击感的问题，新增颜色区分：
  - 工作 (W)：绿色
  - 游戏 (G)：红色
  - 休闲 (L)：蓝色
  - 其他 (O)：灰色
  - 移除 (Del)：棕色

### 变更
- 关于页面作者改为 **YTU22**
- 移除关于页面官网链接
- 删除废弃的 master 分支，仅保留 main 分支

---

## v1.0.3 (2026-05-29)

### 新增功能
- **未分类进程检测列表**：
  - 设置 → 进程名单页面实时列出检测到但未分类的进程
  - 支持快捷键快速分类：W-办公 / G-游戏 / L-休闲 / O-其他 / Delete-移除
  - 支持多选批量分类
- **版本号显示**：托盘菜单、关于页面显示当前版本号

---

## v1.0.2 (2026-05-28)

### 新增功能
- **零依赖 GPU 监控**：
  - NVIDIA 显卡通过 ctypes 调用 NVML
  - AMD/Intel 显卡通过 win32pdh 性能计数器
- **配置热重载**：修改 config.json 后自动生效，无需重启
- **数据库自动清理**：30 天旧数据自动归档并 VACUUM 压缩
- **规则表达式引擎**：支持安全 eval 的自定义提醒规则

---

## v1.0.1 (2026-05-27)

### 新增功能
- **开机自启动**：注册表写入 + 托盘菜单 + 设置页面三入口控制
- **记住窗口位置**：首次右下角，后续恢复上次位置
- **首次运行快捷方式**：自动在桌面创建 VibePet.lnk
- **系统监控面板**：CPU / 内存 / 磁盘 IO / 网速 / GPU 实时显示

---

## v1.0.0 (2026-05-25)

### 初始版本
- 桌面宠物 PNG 渲染，支持 6 种状态动画
- 呼吸浮动、眨眼、点击摇晃动态效果
- 软件使用时长监测（办公/游戏/休闲/其他四分类）
- 健康提醒引擎（游戏沉迷、久坐、深夜防熬夜、正向激励、每日疲劳）
- 随机情绪系统（按时段概率触发 sleep/happy/tired）
- 统计看板（柱状图 + 饼图 + CSV 导出）
- 系统托盘控制
- 窗口锁定 / 鼠标穿透 / 可调大小
